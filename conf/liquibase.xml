<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-2.0.xsd">

    <changeSet id="290620181627" author="Prasanna, Raju">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="markers" schemaName="public"/>
            </not>
        </preConditions>
        <comment>Creating the markers table if not exists in public schema</comment>
        <sql>
            CREATE TABLE IF NOT EXISTS public.markers (
                job_name          TEXT PRIMARY KEY,
                event_record_id   INTEGER NOT NULL,
                category          TEXT NOT NULL,
                table_name        TEXT NOT NULL
            );
        </sql>
    </changeSet>

    <changeSet id="290620181630" author="Prasanna, Raju">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM public.markers WHERE job_name='Obs Data';
            </sqlCheck>
        </preConditions>
        <comment>Adding marker information for obs data job</comment>
        <sql>
            INSERT INTO markers(job_name, event_record_id, category, table_name)
            VALUES('Obs Data', 0, 'Encounter', 'encounter');
        </sql>
    </changeSet>
</databaseChangeLog>