sudo: true

language : java
jdk:
- oraclejdk8

services:
  - docker

jobs:
  include:
    - stage : compile
      script: ./compile.sh
    - stage : test
      script: ./test.sh
    - stage : builddocker
      script: ./builddocker.sh

stages:
      - compile
      - test
      - builddocker
